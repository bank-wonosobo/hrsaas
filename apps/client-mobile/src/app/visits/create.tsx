import FormVisit from "@/features/visit/form-visit";
import { View } from "react-native";

export default function CreateVisit() {
  return (
    <View className="flex-1 bg-gray-50">
      <FormVisit />
    </View>
  );
}
